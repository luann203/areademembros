# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-change-in-production

# Local: SQLite (schema usa file:./dev.db). Não defina VERCEL aqui.
# Vercel: opcional; se não houver DATABASE_URL, o app usa mock (sem banco).
# DATABASE_URL="postgresql://..."  # só para Vercel com Postgres/Neon etc.
